{ config, lib, pkgs, ... }: {
  services = {
    swaync = {
      enable = true;
      settings = {
        positionX = "right";
        positionY = "bottom";
        timeout = 15;
        timeout-low = 5;
        timeout-critical = 0;
        fit-to-screen = true;
        keyboard-shortcuts = true;
        image-visibility = "when-available";
        transition-time = 100;
        hide-on-clear = true;
        hide-on-action = true;
        script-fail-notify = true;
        control-center-margin-top = 2;
        control-center-margin-bottom = 2;
        control-center-margin-right = 2;
        control-center-margin-left = 2;
        notification-icon-size = 45;
        notification-body-image-height = 60;
        notification-body-image-width = 120;
        control-center-width = 360;
        control-center-height = 600;
        notification-window-width = 360;
        widgets = [ "volume" "title" "mpris" "dnd" "notifications" ];
        widget-config = {
          volume = { label = "󰕾 "; };
          title = {
            text = "Notifications";
            clear-all-button = true;
            button-text = "Clear All";
          };
          dnd = { text = "Do Not Disturb"; };
          label = {
            max-lines = 1;
            text = "Notification Center";
          };
          mpris = {
            image-size = 45;
            image-radius = 8;
          };
        };
      };

      style = ''
        * {
        all: unset;
        transition: 200ms ease-out;
        font-size: 8px;
        font-family: "Roboto", "Fira Sans", sans-serif;

        /* Base Colors */
        --bg-primary: rgba(30, 30, 30, 0.8);
        --bg-primary-hover: rgba(40, 40, 40, 0.9);
        --bg-secondary: rgba(25, 25, 25, 0.7);
        --bg-notification: rgba(30, 30, 30, 0.5);

        /* Text Colors */
        --text-primary: #ffffff;
        --text-secondary: #a0a0a0;

        /* Border Colors */
        --border-primary: rgba(255, 255, 255, 0.08);
        --border-secondary: rgba(255, 255, 255, 0.05);

        /* Shadow Colors */
        --shadow-primary: rgba(0, 0, 0, 0.2);
        --shadow-secondary: rgba(0, 0, 0, 0.3);
        --shadow-control-center: rgba(0, 0, 0, 0.25);
        --shadow-notification: rgba(0, 0, 0, 0.15);

        /* Blur Effects */
        --blur-amount: 12px;
        --blur-control-center: 16px;

        }
        .notification-row {
          outline: none;
        }

        .notification {
        background: var(--bg-primary);
        border-radius: 12px;
        margin: 8px;
        box-shadow: 0 4px 12px var(--shadow-primary);
        border: 1px solid var(--border-primary);
        }

        .notification:hover {
         background: var(--bg-primary-hover);
         box-shadow: 0 6px 16px var(--shadow-secondary);
        }

        .floating-notifications.background .notification-row .notification-background {
        background: var(--bg-notification);
        backdrop-filter: blur(var(--blur-amount));
        -webkit-backdrop-filter: blur(var(--blur-amount));
        border-radius: 12px;
        margin: 8px;
        padding: 0;
        box-shadow: 0 4px 12px var(--shadow-notification);
        border: 1px solid var(--border-secondary);
        }

        .control-center {
        background: var(--bg-secondary);
        backdrop-filter: blur(var(--blur-control-center));
        -webkit-backdrop-filter: blur(var(--blur-control-center));
        border: 1px solid var(--border-primary);
        border-radius: 16px;
        padding: 12px;
        box-shadow: 0 8px 24px var(--shadow-control-center);
        }

        .widget-title {
        color: var(--text-primary);
        font-size: 10px;
        font-weight: 500;
        margin-bottom: 8px;
        }

        .widget-dnd {
        color: var(--text-secondary);
        }

        .widget-mpris {
        color: var(--text-primary);
        }
      '';
    };
  };
}
