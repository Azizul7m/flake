return {
	{
		"akinsho/bufferline.nvim",
		-- Bufferline registers BufEnter handlers that can run while Noice is
		-- creating a popup. Load it first so its state is initialized.
		lazy = false,
	},
}
