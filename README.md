# Personal NixOS configuration

This repository is highly personal and tailored to one machine, one user, and
one workflow. It is not intended for anyone else to clone, install, copy, or
use as-is. Paths, hardware settings, secrets, services, and preferences may
be specific to the owner and may not work elsewhere.

## Conflict status

No unresolved merge conflict is present in the current checkout: Git reports
no unmerged paths and no conflict markers. The changes in this commit are
located in `flake.nix`, `host/virtualisation.nix`,
`modules/programs/hypr/hyprland.nix`, and the Neovim files under `src/nvim/`.
